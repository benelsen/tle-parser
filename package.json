{
  "name": "tle-parser",
  "version": "0.1.4",
  "description": "",
  "author": "Ben Elsen <mail@benelsen.com> (https://benelsen.com/)",
  "main": "lib5/index.js",
  "scripts": {
    "build": "babel -d lib5/ lib/",
    "lint": "eslint lib/ test/",
    "test": "babel-tape-runner test/**/*",
    "version": "npm run lint && npm run test && npm run build",
    "prepublish": "npm run lint && npm run test && npm run build"
  },
  "dependencies": {
    "fixedwidth": "^1.1.3",
    "ramda": "^0.18.0"
  },
  "devDependencies": {
    "babel-cli": "^6.1.2",
    "babel-preset-es2015": "^6.1.2",
    "babel-tape-runner": "^1.3.0",
    "eslint": "^1.9.0",
    "tape": "^4.2.2"
  },
  "license": "ISC"
}
